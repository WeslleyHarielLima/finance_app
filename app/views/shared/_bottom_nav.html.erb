<nav class="bottom-nav" role="navigation" aria-label="Menu principal">
  <%= link_to dashboard_path, class: "nav-item #{'active' if current_page?(dashboard_path)}" do %>
    <span class="nav-icon" aria-hidden="true">🏠</span>
    Inicio
  <% end %>

  <%= link_to financial_entries_path, class: "nav-item #{'active' if current_page?(financial_entries_path)}" do %>
    <span class="nav-icon" aria-hidden="true">💰</span>
    Transacoes
  <% end %>

  <%= link_to new_financial_entry_path, class: "nav-item special" do %>
    <span class="nav-icon" aria-hidden="true">+</span>
    Nova
  <% end %>

  <%= link_to reports_path, class: "nav-item #{'active' if current_page?(reports_path)}" do %>
    <span class="nav-icon" aria-hidden="true">📊</span>
    Relatorios
  <% end %>

  <%= link_to profile_path, class: "nav-item #{'active' if current_page?(profile_path)}" do %>
    <span class="nav-icon" aria-hidden="true">👤</span>
    Perfil
  <% end %>
</nav>
